# Pinned dependencies for the Python demos (aster-code.py, sol_agent.py).
# eth-account >= 0.13 is required: encode_typed_data() replaced the older
# encode_structured_data() API that earlier examples imported.
eth-account>=0.13.0
requests>=2.31.0
base58>=2.1.1
PyNaCl>=1.5.0
